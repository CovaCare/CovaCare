import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    justifyContent: "flex-end",
    paddingTop: 19,
    paddingBottom: 1,
  },
  logo: {
    width: 90,
    height: 50,
  },
}); 