import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  fieldTitle: {
    fontSize: 14,
    fontWeight: 600,
    marginBottom: 7,
  },
  input: {
    borderWidth: 1,
    borderColor: "#DDD",
    borderRadius: 8,
    padding: 12,
    marginBottom: 16,
  },
  inputError: {
    borderColor: "#FF5252",
  },
});
