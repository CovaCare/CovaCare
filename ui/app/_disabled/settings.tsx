import { Text, View } from "react-native";
import { styles } from "../(tabs)/styles/SettingsScreen.styles";

const SettingScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>Setting screen</Text>
    </View>
  );
};

export default SettingScreen;
